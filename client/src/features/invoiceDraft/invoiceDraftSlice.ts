import { createSlice } from '@reduxjs/toolkit';

// Placeholder for Phase 3 scaffold. Expanded in Phase 5 with customer + lineItems state.
const slice = createSlice({
  name: 'invoiceDraft',
  initialState: {},
  reducers: {},
});

export default slice.reducer;
